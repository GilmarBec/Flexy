{% extends 'base.html.twig' %}

{% block title %} Produto {% endblock %}

{% block body %}
	<div class="card">
		<div class="card-head border" style="background-color: #f8f8f8; text-align: center;">
			<h1>Produto</h1>
		</div>
		<div class="card-body">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
			<a href="/" class=""></a>
		</div>
	</div>
{% endblock %}